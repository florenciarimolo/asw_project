<%= form_with(model: issue_list, local: true) do |form| %>
  <% if issue_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(issue_list.errors.count, "error") %> prohibited this issue_list from being saved:</h2>

      <ul>
      <% issue_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :issue_list_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
