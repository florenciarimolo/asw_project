<%= form_for @issue_list, :html => {:class => "form-horizontal center"} do |form| %>
  <% if issue_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(issue_list.errors.count, "error") %> prohibited this issue_list from being saved:</h2>

      <ul>
      <% issue_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group col-md-8">
    <label class="required"><%= form.label :name %></label>
    <%= form.text_field :name, id: :issue_list_name, class: "form-control"%>
  </div>

  <div class="actions submit-button col-md-8">
    <%= form.submit "Create List", class: "btn btn-default btn-primary"%>
  </div>
<% end %>
